const r=require("express").Router(),{body}=require("express-validator"),c=require("../controllers/roomController"),p=require("../middleware/auth"),a=require("../middleware/role"),v=require("../middleware/validate");const val=[body("roomTypeId").isMongoId(),body("roomNumber").trim().notEmpty(),body("housekeepingStatus").optional().isIn(["clean","dirty","maintenance"])];r.get("/hotel/:hotelId",c.getRoomsByHotel);r.get("/:id",c.getRoomById);r.post("/",p,a("hotelOwner","admin"),val,v,c.createRoom);r.put("/:id",p,a("hotelOwner","admin"),val,v,c.updateRoom);r.delete("/:id",p,a("hotelOwner","admin"),c.deleteRoom);module.exports=r;
