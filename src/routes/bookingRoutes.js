const r=require("express").Router(),{body}=require("express-validator"),c=require("../controllers/bookingController"),p=require("../middleware/auth"),a=require("../middleware/role"),v=require("../middleware/validate");const val=[body("hotelId").isMongoId(),body("roomTypeId").isMongoId(),body("checkIn").isISO8601(),body("checkOut").isISO8601(),body("guests").isInt({min:1})];r.post("/",p,val,v,c.createBooking);r.get("/my",p,c.getMyBookings);r.get("/:id",p,c.getBookingById);r.put("/:id/status",p,a("hotelOwner","staff","admin"),[body("status").isIn(["Confirmed","Cancelled"])],v,c.updateStatus);r.put("/:id/cancel",p,c.cancelBooking);module.exports=r;
