const r=require("express").Router(),{body,param,query}=require("express-validator"),p=require("../middleware/auth"),a=require("../middleware/role"),v=require("../middleware/validate"),c=require("../controllers/housekeepingController");const filters=[query("housekeepingStatus").optional().isIn(["clean","dirty","maintenance"]),query("hotelId").optional().isMongoId()];r.get("/",p,a("staff","hotelOwner","admin"),filters,v,c.getHousekeeping);r.put("/:id",p,a("staff","hotelOwner","admin"),[param("id").isMongoId(),body("housekeepingStatus").isIn(["clean","dirty","maintenance"])],v,c.updateHousekeeping);module.exports=r;
