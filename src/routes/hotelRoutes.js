const r=require("express").Router(),{body}=require("express-validator"),c=require("../controllers/hotelController"),p=require("../middleware/auth"),a=require("../middleware/role"),v=require("../middleware/validate");const val=[body("name").trim().notEmpty(),body("city").trim().notEmpty(),body("amenities").optional().isArray(),body("rating").optional().isFloat({min:0,max:5})];r.get("/",c.getHotels);r.get("/:id",c.getHotelById);r.post("/",p,a("hotelOwner","admin"),val,v,c.createHotel);r.put("/:id",p,a("hotelOwner","admin"),val,v,c.updateHotel);r.delete("/:id",p,a("hotelOwner","admin"),c.deleteHotel);module.exports=r;
