const r=require("express").Router(),{body}=require("express-validator"),c=require("../controllers/authController"),v=require("../middleware/validate");r.post("/register",[body("name").trim().notEmpty(),body("email").isEmail().normalizeEmail(),body("password").isLength({min:6}),body("role").optional().isIn(["guest","hotelOwner","staff","admin"])],v,c.register);r.post("/login",[body("email").isEmail().normalizeEmail(),body("password").notEmpty()],v,c.login);module.exports=r;
