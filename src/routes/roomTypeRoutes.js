const r=require("express").Router(),{body}=require("express-validator"),c=require("../controllers/roomTypeController"),p=require("../middleware/auth"),a=require("../middleware/role"),v=require("../middleware/validate");const val=[body("hotelId").isMongoId(),body("name").trim().notEmpty(),body("basePrice").isFloat({min:0}),body("totalRooms").isInt({min:1}),body("capacity").isInt({min:1})];r.get("/hotel/:hotelId",c.getRoomTypesByHotel);r.get("/:id",c.getRoomTypeById);r.post("/",p,a("hotelOwner","admin"),val,v,c.createRoomType);r.put("/:id",p,a("hotelOwner","admin"),val,v,c.updateRoomType);r.delete("/:id",p,a("hotelOwner","admin"),c.deleteRoomType);module.exports=r;
