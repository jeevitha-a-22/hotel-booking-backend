const Booking=require("../models/Booking"),{canTransition}=require("../utils/bookingStatus");exports.cancelWithPolicy=async(req,res,next)=>{try{const b=await Booking.findById(req.params.id);if(!b)return res.status(404).json({success:false,message:"Booking not found"});if(!canTransition(b.status,'Cancelled'))return res.status(409).json({success:false,message:"Booking cannot be cancelled"});const d=(new Date(b.checkIn)-new Date())/86400000,rate=d>=7?1:d>=2?.5:0;b.status='Cancelled';b.cancellationRefund=+(b.totalAmount*rate).toFixed(2);await b.save();res.json({success:true,policy:{refundPercentage:rate*100,refundAmount:b.cancellationRefund},booking:b})}catch(e){next(e)}};
