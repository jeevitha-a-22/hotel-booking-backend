const RoomType=require("../models/RoomType"),Booking=require("../models/Booking");exports.searchAvailability=async(req,res,next)=>{try{const{hotelId,checkIn,checkOut,guests=1}=req.query,a=new Date(checkIn),b=new Date(checkOut);if(!checkIn||!checkOut||isNaN(a)||isNaN(b)||a>=b)return res.status(400).json({success:false,message:"Valid checkIn/checkOut are required"});const r=await RoomType.find({hotelId,capacity:{$gte:+guests}}),out=[];for(const x of r){const n=await Booking.countDocuments({roomTypeId:x._id,status:{$in:["Reserved","Confirmed","Checked-in"]},checkIn:{$lt:b},checkOut:{$gt:a}});out.push({roomType:x,availableRooms:Math.max(x.totalRooms-n,0),available:n<x.totalRooms})}res.json({success:true,results:out})}catch(e){next(e)}};
