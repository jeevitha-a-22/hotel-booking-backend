const Booking=require("../models/Booking");exports.getInvoice=async(req,res,next)=>{try{const b=await Booking.findById(req.params.id).populate("guestId","name email").populate("hotelId","name city").populate("roomTypeId","name");if(!b)return res.status(404).json({success:false,message:"Booking not found"});if(b.guestId._id.toString()!==req.user._id.toString()&&!['admin','staff','hotelOwner'].includes(req.user.role))return res.status(403).json({success:false,message:"Not allowed"});if(!b.invoiceNumber){b.invoiceNumber=`INV-${Date.now()}-${String(b._id).slice(-6).toUpperCase()}`;await b.save()}res.json({success:true,invoice:{invoiceNumber:b.invoiceNumber,guest:{name:b.guestId.name,email:b.guestId.email},hotel:{name:b.hotelId.name,city:b.hotelId.city},roomType:b.roomTypeId.name,checkIn:b.checkIn,checkOut:b.checkOut,nights:b.nights,roomCost:b.roomCost,taxes:b.taxes,totalAmount:b.totalAmount,refund:b.cancellationRefund,status:b.status}})}catch(e){next(e)}};
