const Booking=require("../models/Booking"),RoomType=require("../models/RoomType");exports.occupancyReport=async(req,res,next)=>{try{const{hotelId}=req.query;if(!hotelId)return res.status(400).json({success:false,message:"hotelId is required"});const[r,b]=await Promise.all([RoomType.find({hotelId}),Booking.find({hotelId,status:{$in:["Reserved","Confirmed","Checked-in","Checked-out"]}})]);const total=r.reduce((s,x)=>s+x.totalRooms,0),revenue=b.reduce((s,x)=>s+x.totalAmount,0);res.json({success:true,report:{hotelId,totalRooms:total,bookingCount:b.length,occupiedRoomNights:b.reduce((s,x)=>s+x.nights,0),revenue:+revenue.toFixed(2),occupancyRate:total?+(b.length/total*100).toFixed(2):0}})}catch(e){next(e)}};
